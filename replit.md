# Photo Gallery

This is a Flask-based web application with a Python backend.

## Run

The **Start application** workflow serves the project on port 5000 with:

```sh
python3 server.py
```

Open the Replit Preview to view the gallery. Changes to `index.html`, `styles.css`, `script.js`, or `server.py` are available after restarting the server.